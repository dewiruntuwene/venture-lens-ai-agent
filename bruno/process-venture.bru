meta {
  name: "Process Venture (Full Flow)"
  type: "http"
  seq: 4
}

post {
  url: {{base_url}}/process
  body: json
  auth: none
}

body:json {
  {
    "url": "https://anthropic.com"
  }
}

docs {
  Performs the complete flow:
  1. Scrapes the provided URL.
  2. Saves the initial metadata.
  3. Uses AI to generate structured insights.
  4. Updates the database and returns the final result.
}
